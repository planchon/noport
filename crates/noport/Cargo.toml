[package]
name = "noport"
version = "0.1.0"
edition = "2024"

[dependencies]
daemon = { path = "../daemon" }
noport-lib = { path = "../lib" } 
clap = { workspace = true }
tokio = { workspace = true }
anyhow.workspace = true
paris.workspace = true
nix = { version = "0.29", features = ["user"] }  